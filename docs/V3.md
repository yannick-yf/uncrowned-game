# Uncrowned — what v3 is

> **Read this first.** `V2.md` says what the game became on the evening of 2026-09-13;
> this says what it is since the next morning, when the world under it changed.
> `SPECS.md` is the source of truth and answers *what the game should be*; this answers
> *what it is*, which is the question you have if you have just arrived. Delivered
> 2026-09-13 through 2026-09-15: M1a–c, M2a–b, M3a and the M4 cut-over of `MIGRATION_3D.md`.

**The game plays on the world Yannick's brother is building in 3D, and shows nothing
but his.** His terrain, water, forests and burnt Brindle are the ground; the simulation
still thinks in tiles, because his 768 m are baked into a 384 × 384 grid at two metres a
tile. Every person is his traveller sprite until he draws the cast; every building his
library holds is his mesh; what he has not drawn yet stands as a plain block in his
rock paint, and the woods and roads the simulation has and he has not are simply not
there to see — his to plant and lay. Nothing of the 2D game's art is on screen (the
HUD's font aside). Nothing about *what the game is* changed: the five pillars, the
twelve quantities, hardship, the four places and their two states, rank, the readings,
the endings, the journal, French first.

The game is written and played in **French first** and translated into English.

---

## Run it

```bash
godot --headless --path . --import          # once after cloning, and after a new class_name
tools/vendor_workshop.sh                    # once after cloning, and after each delivery of his
tools/run_tests.sh                          # fast suite, ~8 s, on the baked world
tools/run_tests.sh --all                    # everything, ~24 s — before committing
tools/run_tests.sh --procedural --all       # the same suite on the 2D map, ~20 s — before committing too
godot --path . &                            # play it, in 3D; =/- or the wheel zooms
tools/shot.sh /tmp/a.png play 292,290       # one frame of the 3D window, standing at a tile
UNCROWNED_VIEW=2d tools/shot.sh /tmp/b.png map                   # the baked world, flat
godot --headless --path . -s tools/bake_region.gd                # his data + the brief -> content/region.json
godot --headless --path . -s tools/measure_routes.gd             # road / wild / attuned, at his pace
godot --headless --path . -s tools/map_criteria.gd               # MAP_SPEC's twelve, on the baked grid
```

`--headless` never calls `_draw()`, so the suite cannot see the screen. Every visual
claim below was checked with a picture.

---

## What v3 built

| Phase | What it is | Where |
|---|---|---|
| **M1a — anchors** | Where everything stands is data: `content/places.json` holds the eight places, the clearing, the crossings, and an anchor — a place or point plus an offset — for every person, fire, stall and paper. No `.gd` and no cast sheet carries a tile. `test_anchors` fails **by name** on an anchor that resolves nowhere | `core/places.gd`, `Region.resolve`, `content/places.json` |
| **M1b — the bake** | His four landscape files, his geography, his sectors and trees plus `content/bake_brief.json` — our proposals, in his metres, for what his data lacks — become `content/region.json`: terrain kinds, his buildings, the places, the points, the pace. Deterministic (baked twice, compared), hashed, checked in, `--check` in CI | `core/rules/bake_rules.gd`, `core/region_bake.gd`, `tools/bake_region.gd` |
| **M1c — the kit** | The procedural map's settlement stamps became a kit (`Region.scaffold_place`) the bake stands on his terrain for every place he has not built; the brief plants the Thornwood, the fields and the marsh; the road follows his road tiles; the suites say where they stand in the world's terms. A claim the map owes is a **DEBT**, printed and counted apart | `core/region.gd`, `test/test_case.gd`, `tools/test_runner.gd` |
| **M2a — the window** | `view/world3d.gd`: his camera in numbers, the fairy as light, and a fallback ground built from his heights coloured by the baked terrain for a clone without his scenes. The play screen is the bridge: it hands the window one frame dictionary a tick and the window writes nothing back | `view/world3d.gd`, `view/main.gd` |
| **M2b — his scenes** | `tools/vendor_workshop.gd` copies his project's folders into `view3d/workshop/` with their `res://` paths repointed — generated, never committed. The window adopts his map plate (terrain, water, relief, Brindle, forests, mine, sun); his buildings are his meshes; his traveller is everyone | `tools/vendor_workshop.gd`, `view/world3d.gd` |
| **M3a — parity** | What the 2D window shows that needs none of his art: the marks over whoever can see you, embers over fires and kilns, cold kilns for a freed works, the fairy's glow, tents struck, fences gone, the crowd sized — drawn with one soft disc made in code, no pack asset | `view/world3d.gd` |
| **M4 — cut-over** | The baked world is the game; `UNCROWNED_WORLD=procedural` is the 2D map. Walking follows the workshop: 2.5 tiles a second, read from the world's data; six on the 2D map. A save remembers its world | `core/places.gd`, `core/rules/movement_rules.gd`, `core/save_file.gd` |
| **M3b — his art, begun** | The kit's houses, barns, wells, barrels and fences stand as pieces of his library (`kit_library` in the brief); what he has not drawn is a plain block in his rock paint; every person is his traveller. *What stops you must be seen* (2026-09-14): rock at 0.85 of his paint, the clearing's ring left open as a DEBT, footprints shrunk to his piece, the ramparts as blocks; the lens zooms with his camera's numbers | `view/world3d.gd`, `core/region_bake.gd`, `core/rules/bake_rules.gd`, `content/bake_brief.json` |

**43 suites, 431 tests** — green on both worlds. On the baked one the run prints nine
DEBT lines, which are five claims: the road band is measured by three suites, the road
ratio and the clearing's ring by two each. Four OFF lines remain.

**The ironworks and crossings delivery is ingested (2026-09-15, PR #6).** The 27
buildings and 47 props replace the Cinderworks kit; its zone follows the delivered
100 × 104 m bounds. Collision shapes, extracted outside core, keep exterior approaches
and open halls separate from roof envelopes. All five bridges are named and joined by
the delivered regional roads. The window seats walkers on the bridge decks and cools
the delivered furnace and forge effects when the works is freed. Halgrave and the
watchmen stand by a kiln through feature anchors; the bridge guard stands on its deck.
The generated scene copy is 1,507 files, 25.4 MB. Town and bridge frames were inspected.

**His second delivery is ingested (2026-09-16).** The factory is his v4 layout: the
six furnaces stand in an ordered three-by-two grid, sixteen of the twenty-seven
buildings moved — `DortoirNord` right across the site — and the works' zone follows
his widened 110 m bounds, its centre now (312,213). The delivered town is read by his
building ids, so none of that cost content a line. Seven of his crossings are named
and joined, including `PontPorteRoyale` over his new royal moat, whose deck the window
now seats walkers on.

**His sawmill village and his royal mountain city are on screen and not yet in the
simulation.** Fifteen buildings with a working mill wheel and its water channel;
thirty-four buildings, a curtain wall of eleven towers, a switchback climb, a stone
bridge and a moat. They arrive with his own map plate, so the window draws them —
but `village_scierie` is still ignored by the brief and Cairnwell still carries our
scaffold kit on top of his city, the HUD calls the sawmill *les terres sauvages*, and
his walls stop nobody. Reading `sawmill-town.json` and `royal-city.json` the way
`ironworks-town.json` is read is the next piece of ingestion, one apiece; his geography
now names both with `built_scene`, `built_bounds_xz` and `built_buildings`.

**And one rule of the bake changed.** Once he delivered any crossing, the bake took
every wet road tile to be one of his bridges. His royal feeder channel crossed the
King's Road twenty-five tiles from his nearest bridge, three water tiles went down
in silence, and the road to the capital was cut — the King's Road's waypoints jumped
112 tiles in one leg and a walker spent 400 s covering 284 tiles. His bridges run
before the roads, so a tile still wet under a road is one no bridge of his covers:
the bake lays it and the report names it. Beside his own deck it still refuses.

---

## The numbers, and where they came from

| | 2D map | Baked world | Why |
|---|---|---|---|
| Grid | 280 × 200 | **384 × 384 at 2 m** | His 385-sample grid over 768 m |
| Walking | 6 tiles/s | **2.5 tiles/s** | Decision 1: his controller walks at 5 m/s |
| The King's Road, Brindle to the castle approach | 58 s | **136.3 s**, 340.6 tiles | `measure_routes`, 2026-09-15, speeds off; stops before the king |
| The wild line | 70 s | **133.3 s**, 333.1 tiles | Measured 2026-09-15 with speeds off |
| The attuned wild | 67 s | **133.3 s**, 333.1 tiles | Attunement changes no walking time while speeds are off |
| MAP_SPEC's criteria | 10 of 10 | **8 of 10** | Road ratio and the 45–90 s band remain map decisions |
| Road ratio | 1.41 | **1.24** | 367 road tiles / 296 direct; the road measure includes the full route |

## The five debts the suite prints

- **The furnaces are 70 tiles from Brindle's centre**; §4 wants them in Brindle's
  first frame. His to move or the narrative's to adapt — Yannick's spec-review lane.
- **The full road length takes 147 s at his pace**; §4's 45–90 s band was settled for six
  tiles a second and is renegotiated with the map, not defended.
- **The road ratio is 1.24**, below §4's 1.30–1.50 target. The delivered bridge
  connections shorten the route; its shape is the brother's and Yannick's to settle.
- **The clearing's ring of thicket is his to plant** (2026-09-14). The kit's ring stood
  on open grass with nothing to show it, and Yannick walked into it; the bake leaves it
  open wood until his trees stand there, and §4's *one corridor out* is owed, not held.
- **The north his rivers close** (2026-09-14, his rivers v4). Two new tributaries cut off
  a stretch of wild north of the castle, 15 percent of the passable map, with no
  crossing over either; nothing of the cast stands in it. His bridge to add, or
  wilderness nobody needs.

A DEBT is printed with the run and counted apart from failures so the suite stays green
while the brief (`MIGRATION_3D.md` §5) is open. Never a failure turned into a debt to get
green; a debt names something a person has to settle.

---

## What is deliberately not there

- **The wild's price in time, and the music** — switched off for now (Yannick,
  2026-09-14, walking his brother's map: *slow off the path is useless; remove the music
  as well*). `Region.TERRAIN_SLOWS_YOU` and `Sound.MUSIC`, one word each; the tables
  stand and four tests say `OFF` in the run. The first of the 2D game's layers to come
  off while the 3D world is tested; more will (CLAUDE.md, *Testing switches*).
- **His art in place of the blocks** (M3b): tents, boats, stalls, fires, papers,
  the counting house, the keep and towers, the gatehouses, the muster rolls; the
  free-state variants as real scenes; travellers as carts; the sign as a prop at each
  gate; and **the 25 faces** in his style — the one asset every conversation stands in
  front of. Until then everyone is his traveller and twenty-five people share one face,
  while the Cinderworks now has its own furnaces and forges.
- **The map's fill** (M5): Harrowgate, the Muster, Saltmarch, the fields, Cairnwell's
  streets and the castle stand as our kit on his ground; the Thornwood is the brief's;
  the five delivered bridges and their roads are now ingested. The northern tributaries
  still leave wilderness disconnected.
- **The provenance-and-licence validator** for 3D assets and §13 rewritten around it
  wait for the assets they concern.
- **The canopy fading over the player**: the brief's trees are one mesh; his trees will
  do it.
- **Combat**, as in v1 and v2.

---

## The debug tools, all gated on `OS.has_feature("debug")`

`T` skips an in-game day · `M` opens the map of Erileo, which is now his map · the
journal's last page lists who is where · `tools/shot.sh` photographs any screen ·
`UNCROWNED_FREE=zone[,zone]` frees places for a frame. Two selectors that are not debug
tools and not gated: `UNCROWNED_WORLD=procedural` (the 2D map) and `UNCROWNED_VIEW=2d`
(the baked world, flat).

---

## Where to read next

| | |
|---|---|
| `CLAUDE.md` | How to work here, the twelve invariants, the two worlds and the DEBT rule — and how to commit |
| `docs/MIGRATION_3D.md` | The plan and the record: what each phase delivered, the brief for his map (§5), the six decisions (§9) |
| `content/bake_brief.json` | Every proposal we made on his map, in his metres, with its reason |
| `SPECS.md` §18 | The roadmap as finished work; every ✅ row dated 2026-09-13 or 14 is v3 |
| `docs/V2.md` | What v3 was built on |
